import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
const tables=['profiles','areas','habits','habit_logs','objectives','objective_items','consumptions','consumption_logs','meals','water_logs','workouts','incomes','expenses','budgets','financial_goals','goal_contributions','frequent_actions','reminders','ai_conversations','ai_messages','ai_memory','home_layout','navigation_layout','score_snapshots','file_assets','subscriptions','devices','analytics_events','ai_settings','ai_action_audit','push_subscriptions','notification_preferences'];
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const auth=req.headers.get('Authorization')??'';const supabase=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}});const {data:{user},error}=await supabase.auth.getUser();if(error||!user)throw new Error('No autenticado');const out:any={exportedAt:new Date().toISOString(),userId:user.id};for(const t of tables){const {data,error:e}=await supabase.from(t).select('*');if(e) throw e;out[t]=data??[];}return new Response(JSON.stringify(out),{headers:{...cors,'Content-Type':'application/json','Content-Disposition':`attachment; filename="impulse-export-${user.id}.json"`}});}catch(e:any){return new Response(JSON.stringify({error:e.message}),{status:400,headers:{...cors,'Content-Type':'application/json'}})}});
